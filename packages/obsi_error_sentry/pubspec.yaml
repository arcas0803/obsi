name: obsi_error_sentry
description: Production Sentry error reporting exporter for the Obsi SDK.
version: 1.1.0
repository: https://github.com/arcas0803/obsi
issue_tracker: https://github.com/arcas0803/obsi/issues
topics: [observability, errors, sentry, crash-reporting]

environment:
  sdk: ^3.12.1

resolution: workspace

dependencies:
  obsi: ^1.1.0
  sentry: ^9.27.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
