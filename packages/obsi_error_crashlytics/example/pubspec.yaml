name: obsi_crashlytics_example
description: Android and iOS integration host for obsi_error_crashlytics.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.12.1

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  obsi: ^1.0.0
  obsi_error_crashlytics: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
