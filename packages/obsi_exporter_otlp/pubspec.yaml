name: obsi_exporter_otlp
description: Production OTLP HTTP/Protobuf and HTTP/JSON exporters for Obsi.
version: 1.0.1
repository: https://github.com/arcas0803/obsi
issue_tracker: https://github.com/arcas0803/obsi/issues
topics: [observability, opentelemetry, otlp, tracing, metrics]

environment:
  sdk: ^3.12.1

resolution: workspace

dependencies:
  http: ^1.6.0
  obsi: ^1.0.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
