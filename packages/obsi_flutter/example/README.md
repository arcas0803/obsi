# Obsi navigation example

Minimal Flutter host used to verify `ObsiNavigatorObserver` with real push and
pop transitions on Android and iOS.

```sh
flutter test integration_test/navigation_integration_test.dart -d <device>
```
