name: obsi
description: Production-focused tracing, logging, metrics, errors, and W3C context propagation for pure Dart.
version: 1.0.0
repository: https://github.com/arcas0803/obsi
issue_tracker: https://github.com/arcas0803/obsi/issues
topics: [observability, tracing, logging, metrics, errors]

environment:
  sdk: ^3.12.1

resolution: workspace

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
