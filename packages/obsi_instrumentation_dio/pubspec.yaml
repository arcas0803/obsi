name: obsi_instrumentation_dio
description: Production Dio client tracing, metrics, and context propagation for Obsi.
version: 1.0.0
repository: https://github.com/arcas0803/obsi
issue_tracker: https://github.com/arcas0803/obsi/issues
topics: [observability, dio, http, tracing, metrics]

environment:
  sdk: ^3.12.1

resolution: workspace

dependencies:
  dio: ^5.9.0
  obsi: ^1.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
