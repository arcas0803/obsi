name: obsi_cli_example
description: Complete Obsi configuration for a Dart command-line application.
publish_to: none

environment:
  sdk: ^3.12.1

resolution: workspace

dependencies:
  obsi: ^1.1.0
  obsi_exporter_otlp: ^1.1.0

dev_dependencies:
  lints: ^6.0.0
